import type { Localized } from "#i18n";

/** Facts shown under the home hero. */
export const profile = {
  focus: { uz: "Backend, arxitektura, masshtablash", en: "Backends, architecture, scaling" } satisfies Localized,
  stack: ["TypeScript", "Node.js", "Go"],
};
